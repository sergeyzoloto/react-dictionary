import React from 'react';
import { Route, Routes } from 'react-router-dom';

const CustomRouter = () => {
  return (
    <Routes>
      <Route path="/" element={<></>} />
    </Routes>
  );
};

export default CustomRouter;
